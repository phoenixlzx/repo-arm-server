module.exports = {
    // pkginfopath: "/home/www", // pkginfo.db file path, without the last slash "/". Currently not usable.
    siteurl: "http://repo-arm.archlinuxcn.org"  // site url, for returning package download links.
}
