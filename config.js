module.exports = {
    // pkginfopath: "/home/www", // pkginfo.db file path, without the last slash "/". Currently not usable.
    downloadurl: "http://repo-arm-download.archlinuxcn.org"  // site url, for returning package download links.
}
